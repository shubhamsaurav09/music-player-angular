import { SecondsToMinutePipe } from './seconds-to-minute.pipe';

describe('SecondsToMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
